#align(center)[= Linear Algebra ex 1]

$space$
$space$


Vector $arrow(v)$ has a tail at $(-2,4)$ and a head at $(4,5)$ Format $v = vec(v 1, v 2)$

$arrow v 1 = 6, v 2 = 1 => vec(6, 1)$

#align(center)[Column Vector $1 dot n => vec(1, 2)$]
#align(center)[row Vector $1 dot n => mat(1, 2)$]

#align(center)[Square Matrix $n dot m=> mat(1, 2; 1, 2)$]


#align(center)[Matrix addition Element wise

  not possible$=>mat(1, 2; 1, 2) dot mat(1, 2)$]

== Zero Matrix
#align(center)[ For every $m dot x =>$ $bold(0)_(m,n) dot A = A$]

== Inverse Element for addition
$ A_(i,j) + -(A_(i,j)) = 0 $

== Scalar Multiplication


$ A = mat(1, 3; 0, 2), beta = 2 $

$ A dot beta = mat(2, 6; 0, 4) $

== Matrix multiplication
$ A dot B != B dot A $
